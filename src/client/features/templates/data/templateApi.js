// Placeholder for API calls to fetch template data (if needed in the future)
export const fetchTemplates = async () => {
    // For now, we're hardcoding the template types
    return [
        { id: 'general-power-of-attorney', name: 'General Power of Attorney' },
        { id: 'appointment-letter', name: 'Appointment Letter' },
    ];
};